# v0.1.2 — 数据层 + AI Settings

**依赖：** 0.1.1  
**对应里程碑：** M1  
**目标：** SQLite 可读写；AI Provider 配置可保存、可脱敏读取、可连通性探测。

---

## 本版本负责（In Scope）

- SQLAlchemy 模型与建库脚本（本版本至少落地 `ai_settings`；其余表可先建空壳或一并建好但不暴露业务 API）
- Settings Service + Settings API
- `LLMProvider` 抽象（厂商不写死）
- API Key 本地加密存储

## 本版本不负责（Out of Scope）

- 账号 / 内容 / 任务 API
- Agent 执行、浏览器操作
- 前端 Settings 页（可 curl/API 验收）

---

## 架构边界

```text
api/settings.py  →  services/settings_service.py  →  db/models(ai_settings)
                                              ↘
                                         llm/provider.py（抽象）
```

- API 层不直接碰加密细节  
- Service 不绑定某一家 SDK 的业务分支到路由里  
- Key 禁止写入日志、禁止明文出现在 API 响应（仅脱敏）

---

## 待办清单

### 数据

- [ ] `db/session.py`：SQLite 引擎与 Session
- [ ] `db/models.py`：至少 `AiSettings`；建议同步建好后续表结构但本版本不接业务
- [ ] `scripts/init_db.py`：一键建表
- [ ] （可选）Alembic；首版可用建表脚本代替

### Settings

- [ ] `GET /api/settings/ai`：返回 provider / base_url / model / api_key 脱敏
- [ ] `PUT /api/settings/ai`：保存配置
- [ ] `POST /api/settings/ai/test`：发一条最小 chat，验证连通
- [ ] API Key：Fernet（或同等）本地加密；密钥文件放本机 `data/`，不进仓库

### LLM 抽象

- [ ] `LLMProvider` 接口：`chat(messages) -> str`
- [ ] 至少实现一个：`OpenAICompatibleProvider`（覆盖 OpenAI / 兼容网关）
- [ ] Provider 由 settings 中的 `provider` + `base_url` 选择，业务不写死厂商名

### 测试

- [ ] 单元测试：settings 读写、Key 脱敏、加密往返

---

## 验收标准

1. 写入 AI 配置后重启进程仍可读取  
2. `/api/settings/ai/test` 在有效 Key 下返回模型回复摘要  
3. 响应与日志中看不到完整 API Key  
4. 更换 `base_url` / `model` 无需改代码  

## 完成定义

Settings 可测通 → 进入 **0.1.3**
