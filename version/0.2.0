# v0.2.0 — MVP 冻结发布

**依赖：** 0.1.8 全部完成  
**对应里程碑：** MVP DoD  
**目标：** 冻结第一阶段产品能力；证明核心假设成立；固化「自研业务 + 复用执行层」边界。

---

## MVP 一句话

> 运行在用户本地电脑上的 AI Social Media Operator：双击启动后，首次「登录并启用」做人机校验，账号池驱动的发布由 **stagehand**（与登录同一 Profile）全自动无人值守完成。

---

## 架构冻结图（允许范围）

```text
UI → API → Services（自研）
              ├─ Settings / LLM 连接池
              ├─ Account Pool → Browser Profile (data/profiles/...)
              ├─ Content Pool → Content Variant
              └─ Publish Queue → Scheduler
                                    ↓
                              Channel (TikTok + Generic)
                                    ↓
                              AgentAdapter (stagehand 默认)
                                    ↓
                              Playwright persistent profile
```

备选：`browser_use` → `stagehand` 降级；`chrome_devtools` 用于平台偏好（如 RedNote），程序可自动起 CDP。  
**冻结期内默认不纳入：** CustomAgent、OCR/PyAutoGUI 主路径、官方 API 排程整仓。

---

## MVP 验收勾选

### 闭环能力

- [ ] 一键启动：`python -m app.launcher` 或 `scripts/start.ps1`
- [ ] 账号「登录并启用」→ ACTIVE（首次人机仅此一次）
- [ ] 视频入队 → **stagehand** 全自动发布 → SUCCESS + 平台可见
- [ ] History 有步骤与截图；Pause / Stop 可用
- [ ] 连续冒烟 ≥3 次 SUCCESS

### 解耦与质量

- [x] 登录与发布共用 `data/profiles/...`
- [x] 一键启动器（无 reload、自动开 UI）
- [x] 自检 + `POST /api/health/heal` 自愈
- [x] Dashboard 就绪态默认折叠

---

## 核心假设验证结论

> 本地 Agent 能否在一键启动、Profile 会话连续的前提下，可靠完成社交媒体发布？

- [ ] **成立**（连续冒烟 ≥3/3 + 平台可见）
- [x] **部分成立**（2026-08-27 更新）
  - **已证明：** 一键启动、登录并启用、stagehand 默认与 Profile 对齐、CDP 自动托管（降级场景）、自检自愈
  - **未证明：** 本机连续真实发布成功率（需 `smoke_publish.py --runs 3`）
- [ ] **不成立**

详见 [KNOWN_ISSUES.md](./KNOWN_ISSUES.md)。

---

## 完成定义

验收勾选全部完成 + 冒烟数据 → **MVP 0.2.0 完成**。
