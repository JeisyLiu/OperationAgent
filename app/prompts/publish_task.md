当前浏览器已经登录目标社交平台账号。

请完成以下任务：

目标：发布内容到 {platform}。

平台首页：{home_url}
发布/上传入口：{upload_url}

视频本地路径：{media_path}
（若无视频路径，则按图文或纯文本发布流程处理）

标题：{title}
正文：{caption}
标签：{hashtags}
分区/版块：{section}

执行要求：

1. 只使用当前已打开且已登录的浏览器会话。
2. 优先打开「发布/上传入口」；若不可用则导航到平台首页再进入发布流程。
3. 采用 Observe → Act → Verify 循环，每步操作后重新观察页面。
4. 完成上传（如有媒体）、填写文案、选择分区（如适用）、发布。
5. 发布成功后返回：status=SUCCESS，并附上可见的成功依据（URL 或页面文案）。
6. 若遇到登录失效、人机校验/验证码、无法识别的阻断，立即停止，返回 status=FAILED 与原因。
7. 不要尝试破解验证码或绕过安全机制；人机校验只应由用户在首次（或重新）登录时完成。
8. 正常发布路径中不要等待人工确认或 Take Over。
